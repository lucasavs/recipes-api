type Recipe = {
    id: number,
    title: string,
    makingTime: string,
    serves: string,
    ingredients: string,
    cost: number,
    createdAt: Date,
    UploadedAt: Date
}

export default Recipe